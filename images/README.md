# README screenshots

Console screenshots referenced from the main README. Referenced by absolute
`raw.githubusercontent.com` URL rather than a relative path, because the Apify
Store renders the README on apify.com, where a relative path would not resolve.

| File | Shows |
|---|---|
| `input-search.png` | Search section: Search Queries, Location, Start URLs, Maximum Items |
| `input-filters.png` | Filters + Deep scraping: date, job type, experience, workplace, Show Salary, deep-scrape toggles |
| `input-proxy-performance.png` | Proxy and performance: proxy config, concurrency, rate, retries, delays |

Keep these filenames exactly — the README links to them by name.
